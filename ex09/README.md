# ft_package

A sample test package for the 42 Python piscine.

This package provides a single function:

```python
from ft_package import count_in_list

print(count_in_list(["toto", "tata", "toto"], "toto"))  # -> 2
